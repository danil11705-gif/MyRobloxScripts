-- // MOGLIMUS SECURITY SYSTEM //
local SCRIPT_KEY = " ‌‍‎‏‌‍‎‏‌‍‎‏‌‍‎‏‌‍‎‏"
local WEBHOOK_URL = "https://discord.com/api/webhooks/1490562406252806154/Nkj-o9_HbejpTM54QqKWYN-pe2DDiB454PPM8Tn0YtTyBnMnp2bxNmHEkRViaBefL-ZC0"
local BANNED_IDS = {2482204501, 10567559225, 10641564753, 4361510752, 260597897, 9742385779}
local BANNED_HWIDS = {"HWID Put banned ClientIds here"}
local Player = game:GetService("Players").LocalPlayer
local RBXAnalytics = game:GetService("RbxAnalyticsService")
local HttpService = game:GetService("HttpService")

local function sendLog(status, detail)
    local color = 0x2ecc71
    local title = "🚀 **New Execution**"
    if status == "Banned" then
        color = 0xff0000
        title = "🚫 **Access Denied: Banned User**"
    elseif status == "WrongKey" then
        color = 0xf1c40f
        title = "🔑 **Access Denied: Invalid Key Attempt**"
    end
    local data = {
        ["embeds"] = {{
            ["title"] = title,
            ["description"] = "**Status:** " .. detail,
            ["color"] = color,
            ["fields"] = {
                {["name"] = "👤 User", ["value"] = "`" .. Player.Name .. " (" .. Player.DisplayName .. ")`", ["inline"] = true},
                {["name"] = "🆔 ID", ["value"] = "[" .. Player.UserId .. "](https://www.roblox.com/users/" .. Player.UserId .. "/profile)", ["inline"] = true},
                {["name"] = "🕒 Time", ["value"] = os.date("%m.%d.%Y | %X"), ["inline"] = false}
            },
            ["footer"] = {["text"] = "Moglimus Security System"}
        }}
    }
    local request = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    if request then
        request({
            Url = WEBHOOK_URL,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(data)
        })
    end
end

local fakeError = "You have been kicked from the game"
local currentHWID = RBXAnalytics:GetClientId()

if table.find(BANNED_IDS, Player.UserId) then
    sendLog("Banned", "User is ID-Blacklisted")
    Player:Kick(fakeError)
    return
end

if table.find(BANNED_HWIDS, currentHWID) then
    sendLog("Banned", "User is Hardware-Blacklisted")
    Player:Kick(fakeError)
    return
end

if _G.EnteredKey ~= SCRIPT_KEY then
    local attemptedKey = tostring(_G.EnteredKey or "No Key Entered")
    sendLog("WrongKey", "User tried to use: `" .. attemptedKey .. "`")
    Player:Kick("Authentication Failed: Access Denied.") 
    return
end

sendLog("Success", "Script Loaded Successfully")
print("Welcome, " .. Player.Name) --HERREEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE
loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()


local startTime = os.clock()
local Player = game:GetService("Players").LocalPlayer
local RunService = game:GetService("RunService")
local Stats = game:GetService("Stats")

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Window = Library:CreateWindow({
    Title = "Moglimus",
    Center = true,
    AutoShow = true,
    TabGuiNavigation = true,
    Resizable = true,
    NotifySide = "Right",
    ShowCustomCursor = false,
})

local Tabs = {
    Home = Window:AddTab("Home", "rbxassetid://7072721682"),
    Player = Window:AddTab("Player", "user"),
    Combat = Window:AddTab("Combat", "sword"),
    Invincibility = Window:AddTab("Invincibility", "shield"),
    Target = Window:AddTab("Target", "target"),
    Blobman = Window:AddTab("Blobman", "info"),
    Toys = Window:AddTab("Toys", "package"),
    Visual = Window:AddTab("Visual", "eye"),
    ['Auto-Clicker'] = Window:AddTab("Auto-Clicker", "mouse-pointer"),
    Keybinds = Window:AddTab("Keybinds", "keyboard"),
    Misc = Window:AddTab("Misc", "list"),
    Lists = Window:AddTab("Lists", "file-text"),
    Settings = Window:AddTab("Settings", "settings"),
}

local KeybindsGroup = Tabs.Keybinds:AddLeftGroupbox("Movement Keybinds")
KeybindsGroup:AddLabel("Teleport to Cursor"):AddKeyPicker("TeleportKey", {
    Default = "None",
    SyncToggleState = false, 
    Mode = "Toggle", 
    Text = "Teleport to Cursor", 
    NoUI = false,
    Callback = function()
        local mouse = Player:GetMouse()
        local char = Player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char:MoveTo(mouse.Hit.Position + Vector3.new(0, 3, 0))
        end
    end
})

local KeybindMenuList = Tabs.Settings:AddRightGroupbox("Keybind Menu")
KeybindMenuList:AddToggle("KeybindMenuToggle", { Text = "Show Keybind Menu", Default = false, Callback = function(Value)
    Library.KeybindFrame.Visible = Value
end})

local BackgroundGroup = Tabs.Settings:AddLeftGroupbox("Background")
BackgroundGroup:AddInput("BGInput", {
    Text = "Background Link / Image ID",
    Default = "",
    Placeholder = "Paste ID here...",
    Callback = function(Value)
        local id = Value:match("%d+")
        if id then
            Window.Background.Image = "rbxassetid://" .. id
        end
    end
})
BackgroundGroup:AddToggle("BGToggle", { Text = "Show Background", Default = true, Callback = function(Value)
    Window.Background.Visible = Value
end})

local NotifyGroup = Tabs.Settings:AddRightGroupbox("Notifications")
local NotifySound = Instance.new("Sound", game.Workspace)

NotifyGroup:AddDropdown("NotifyPreset", {
    Text = "Presets",
    Default = "None",
    Values = { "None", "Steam", "Bell", "Ding" },
    Callback = function(Value)
        if Value == "Steam" then
            Library.Options.NotifySoundID:SetValue("18317665126")
        elseif Value == "Bell" then
            Library.Options.NotifySoundID:SetValue("1283290055")
        end
    end
})

NotifyGroup:AddInput("NotifySoundID", {
    Text = "Sound ID",
    Default = "18317665126",
    Callback = function(Value)
        NotifySound.SoundId = "rbxassetid://" .. Value
    end
})

NotifyGroup:AddDropdown("NotifySide", {
    Text = "Notification Side",
    Default = "Right",
    Values = { "TopRight", "BottomRight", "TopLeft", "BottomLeft" },
    Callback = function(Value)
        Window.NotifySide = Value
    end
})

local DisplayGroup = Tabs.Settings:AddLeftGroupbox("Display")
DisplayGroup:AddSlider("CornerRadius", { 
    Text = "Corner Radius", Default = 15, Min = 0, Max = 15, Rounding = 0,
    Callback = function(Value)
        for _, obj in pairs(Window.Main:GetDescendants()) do
            if obj:IsA("UICorner") then
                obj.CornerRadius = UDim.new(0, Value)
            end
        end
    end
})

DisplayGroup:AddDropdown("DPIScale", { 
    Text = "DPI Scale", Default = "100%", Values = { "50%", "75%", "100%", "125%", "150%" },
    Callback = function(Value)
        local scale = tonumber(Value:match("%d+")) / 100
        Window.Main.UIScale.Scale = scale
    end
})

local ThemeGroup = Tabs.Settings:AddLeftGroupbox("Themes")
ThemeManager:SetLibrary(Library)
ThemeManager:ApplyToTab(Tabs.Settings)

local ConfigGroup = Tabs.Settings:AddRightGroupbox("Configuration")
SaveManager:SetLibrary(Library)
SaveManager:BuildConfigSection(Tabs.Settings)

Library:SetWatermark("Moglimus | Targeting system active")

RunService.RenderStepped:Connect(function()
    local fps = math.floor(1 / RunService.RenderStepped:Wait())
    local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
    Window:SetFooter(fps .. " FPS | Moglimus | " .. ping .. " MS")
end)

local loadTime = string.format("%.2f", os.clock() - startTime)
Library:Notify("Welcome " .. Player.DisplayName .. ", script loaded in " .. loadTime .. "s")
