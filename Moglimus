-- // MOGLIMUS SECURITY & UI //
local SCRIPT_KEY = " ‌‍‎‏‌‍‎‏‌‍‎‏‌‍‎‏‌‍‎‏"
local WEBHOOK_URL = "https://discord.com/api/webhooks/1490562406252806154/Nkj-o9_HbejpTM54QqKWYN-pe2DDiB454PPM8Tn0YtTyBnMnp2bxNmHEkRViaBefL-ZC"

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local HttpService = game:GetService("HttpService")

-- Webhook Funktion (Fixiert)
local function sendLog(status, detail)
    local request = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    if request then
        pcall(function()
            request({
                Url = WEBHOOK_URL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode({
                    ["embeds"] = {{
                        ["title"] = "Moglimus Execute",
                        ["description"] = detail,
                        ["color"] = (status == "Success" and 0x2ecc71 or 0xff0000),
                        ["fields"] = {
                            {["name"] = "Player", ["value"] = LP.Name, ["inline"] = true},
                            {["name"] = "ID", ["value"] = tostring(LP.UserId), ["inline"] = true}
                        }
                    }}
                })
            })
        end)
    end
end

-- Key Check
if _G.EnteredKey ~= SCRIPT_KEY then
    sendLog("Error", "Falscher Key Versuch")
    LP:Kick("Moglimus: Wrong Key!")
    return
end

sendLog("Success", "Script geladen")

-- // UI INITIALISIERUNG //
local Obsidian = loadstring(game:HttpGet("https://raw.githubusercontent.com/teppyboy/Obsidian/main/Source.lua"))()

local Window = Obsidian:CreateWindow({
    Name = "Moglimus",
    Theme = "Default",
    Footer = "discord.gg/Fnt5g27A"
})

-- Deine Tabs (Umbenannt, damit sie das System nicht stören)
local Tabs = {
    Home = Window:CreateTab("Home", "rbxassetid://10734882786"),
    Player = Window:CreateTab("Player", "rbxassetid://10747373176"),
    Combat = Window:CreateTab("Combat", "rbxassetid://10709819149"),
    Invincibility = Window:CreateTab("Invincibility", "rbxassetid://10723346959"),
    Target = Window:CreateTab("Target", "rbxassetid://10709751939"),
    Blobman = Window:CreateTab("Blobman", "rbxassetid://10709810534"),
    Toys = Window:CreateTab("Toys", "rbxassetid://10723352353"),
    Visual = Window:CreateTab("Visual", "rbxassetid://10709752906"),
    AutoClicker = Window:CreateTab("Auto-Clicker", "rbxassetid://10709794351"),
    Misc = Window:CreateTab("Misc", "rbxassetid://10709813243"),
    Settings = Window:CreateTab("Settings", "rbxassetid://10704950315")
}

--- STANDARD SETTINGS TAB ---

local MainSec = Tabs.Settings:CreateSection("Menu Settings")

MainSec:CreateKeybind("UI Toggle", "RightControl", function()
    Window:Toggle()
end)

MainSec:CreateToggle("Watermark", true, function(state)
    -- Hier könnte man das Watermark an/ausschalten
end)

local ThemeSec = Tabs.Settings:CreateSection("Themes")

ThemeSec:CreateColorPicker("Accent Color", Color3.fromRGB(168, 85, 247), function(color)
    Window:SetAccentColor(color)
end)

local ConfigSec = Tabs.Settings:CreateSection("Configuration")

ConfigSec:CreateButton("Destroy UI", function()
    Window:Destroy()
end)

--- WATERMARK (FPS/Ping) ---
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Frame = Instance.new("Frame", ScreenGui)
local Label = Instance.new("TextLabel", Frame)
local Corner = Instance.new("UICorner", Frame)
local Stroke = Instance.new("UIStroke", Frame)

Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Frame.BackgroundTransparency = 0.2
Frame.Position = UDim2.new(0.5, -100, 0.9, 0)
Frame.Size = UDim2.new(0, 200, 0, 25)
Corner.CornerRadius = UDim.new(0, 8)
Stroke.Color = Color3.fromRGB(168, 85, 247)

Label.Size = UDim2.new(1, 0, 1, 0)
Label.BackgroundTransparency = 1
Label.TextColor3 = Color3.fromRGB(255, 255, 255)
Label.TextSize = 14
Label.Font = Enum.Font.Code

task.spawn(function()
    while task.wait(0.5) do
        local fps = math.floor(1/game:GetService("RunService").RenderStepped:Wait())
        local ping = math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue())
        Label.Text = "Moglimus | " .. fps .. " FPS | " .. ping .. " MS"
    end
end)

-- Infinite Yield im Hintergrund laden
task.spawn(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end)
