-- // MOGLIMUS SECURITY SYSTEM //
local SCRIPT_KEY = " ‌‍‎‏‌‍‎‏‌‍‎‏‌‍‎‏‌‍‎‏"
local WEBHOOK_URL = "https://discord.com/api/webhooks/1490562406252806154/Nkj-o9_HbejpTM54QqKWYN-pe2DDiB454PPM8Tn0YtTyBnMnp2bxNmHEkRViaBefL-ZC0"
local BANNED_IDS = {2482204501, 10567559225, 10641564753, 4361510752, 260597897, 9742385779}
local BANNED_HWIDS = {"HWID Put banned ClientIds here"}
local Player = game:GetService("Players").LocalPlayer
local RBXAnalytics = game:GetService("RbxAnalyticsService")
local HttpService = game:GetService("HttpService")

local function sendLog(status, detail)
    local color = 0x2ecc71
    local title = "🚀 **New Execution**"
    if status == "Banned" then
        color = 0xff0000
        title = "🚫 **Access Denied: Banned User**"
    elseif status == "WrongKey" then
        color = 0xf1c40f
        title = "🔑 **Access Denied: Invalid Key Attempt**"
    end
    local data = {
        ["embeds"] = {{
            ["title"] = title,
            ["description"] = "**Status:** " .. detail,
            ["color"] = color,
            ["fields"] = {
                {["name"] = "👤 User", ["value"] = "`" .. Player.Name .. " (" .. Player.DisplayName .. ")`", ["inline"] = true},
                {["name"] = "🆔 ID", ["value"] = "[" .. Player.UserId .. "](https://www.roblox.com/users/" .. Player.UserId .. "/profile)", ["inline"] = true},
                {["name"] = "🕒 Time", ["value"] = os.date("%m.%d.%Y | %X"), ["inline"] = false}
            },
            ["footer"] = {["text"] = "Moglimus Security System"}
        }}
    }
    local request = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    if request then
        request({
            Url = WEBHOOK_URL,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(data)
        })
    end
end

local fakeError = "You have been kicked from the game"
local currentHWID = RBXAnalytics:GetClientId()

if table.find(BANNED_IDS, Player.UserId) then
    sendLog("Banned", "User is ID-Blacklisted")
    Player:Kick(fakeError)
    return
end

if table.find(BANNED_HWIDS, currentHWID) then
    sendLog("Banned", "User is Hardware-Blacklisted")
    Player:Kick(fakeError)
    return
end

if _G.EnteredKey ~= SCRIPT_KEY then
    local attemptedKey = tostring(_G.EnteredKey or "No Key Entered")
    sendLog("WrongKey", "User tried to use: `" .. attemptedKey .. "`")
    Player:Kick("Authentication Failed: Access Denied.") 
    return
end

sendLog("Success", "Script Loaded Successfully")
print("Welcome, " .. Player.Name) --HERREEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE
loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()

-- // AB HIER GEFIXTER UI TEIL //
local startTime = os.clock()
local RunService = game:GetService("RunService")
local Stats = game:GetService("Stats")

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Window = Library:CreateWindow({
    Title = "Moglimus",
    Center = true,
    AutoShow = true,
    Resizable = true,
    NotifySide = "Right",
    ShowCustomCursor = false,
})

-- Sound Helper für Notifications
local function playNotifySound()
    local soundId = "18317665126"
    if Library.Options.SoundId then
        soundId = Library.Options.SoundId.Value:match("%d+") or "18317665126"
    end
    local s = Instance.new("Sound", game:GetService("SoundService"))
    s.SoundId = "rbxassetid://" .. soundId
    s:Play()
    game:GetService("Debris"):AddItem(s, 2)
end

local Tabs = {
    Home = Window:AddTab("Home", "home"), -- "home" sorgt dafür, dass das Icon Lila (#7d55ff) wird
    Player = Window:AddTab("Player", "user"),
    Combat = Window:AddTab("Combat", "sword"),
    Invincibility = Window:AddTab("Invincibility", "shield"),
    Target = Window:AddTab("Target", "target"),
    Blobman = Window:AddTab("Blobman", "info"),
    Toys = Window:AddTab("Toys", "package"),
    Visual = Window:AddTab("Visual", "eye"),
    ['Auto-Clicker'] = Window:AddTab("Auto-Clicker", "mouse-pointer"),
    Keybinds = Window:AddTab("Keybinds", "keyboard"),
    Misc = Window:AddTab("Misc", "list"),
    Lists = Window:AddTab("Lists", "file-text"),
    Settings = Window:AddTab("Settings", "settings"),
}

local MovementGroup = Tabs.Keybinds:AddLeftGroupbox("Movement")
-- FIX: Keypicker für Teleport (KeyPress statt Toggle)
MovementGroup:AddLabel("Teleport to Cursor"):AddKeyPicker("TeleportKey", {
    Default = "None",
    NoUI = false,
    Text = "Teleport",
    Mode = "KeyPress", 
    Callback = function()
        local mouse = Player:GetMouse()
        if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
            Player.Character.HumanoidRootPart.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0, 3, 0))
        end
    end
})

local MenuSettings = Tabs.Settings:AddLeftGroupbox("Menu")
MenuSettings:AddLabel("Menu keybind"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Menu keybind" })
Library.ToggleKeybind = Library.Options.MenuKeybind

MenuSettings:AddToggle("ShowKeybinds", { Text = "Show Keybind Menu", Default = false, Callback = function(Value) Library.KeybindFrame.Visible = Value end })
MenuSettings:AddToggle("Watermark", { Text = "Show Watermark", Default = true, Callback = function(Value) Library:SetWatermarkVisibility(Value) end })

local VisualSettings = Tabs.Settings:AddLeftGroupbox("Display")
-- FIX: Corner Radius
VisualSettings:AddSlider("CornerRadius", { 
    Text = "Corner Radius", Default = 15, Min = 0, Max = 15, Rounding = 0,
    Callback = function(Value)
        pcall(function()
            for _, obj in pairs(Window.Main:GetDescendants()) do
                if obj:IsA("UICorner") then obj.CornerRadius = UDim.new(0, Value) end
            end
        end)
    end
})

-- FIX: UI Scale (DPI)
VisualSettings:AddDropdown("UIScale", { 
    Text = "UI Scale (DPI)", Default = "100%", Values = { "50%", "75%", "100%", "125%", "150%" },
    Callback = function(Value)
        local scale = tonumber(Value:match("%d+")) / 100
        local uiScale = Window.Main:FindFirstChildOfClass("UIScale") or Instance.new("UIScale", Window.Main)
        uiScale.Scale = scale
    end
})

local BgGroup = Tabs.Settings:AddRightGroupbox("Background")
-- FIX: Image ID
BgGroup:AddInput("BgId", { 
    Text = "Image ID", 
    Default = "", 
    Callback = function(Value) 
        local id = Value:match("%d+")
        if id and Window.Main:FindFirstChild("Background") then
            Window.Main.Background.Image = "rbxassetid://" .. id
        end
    end 
})

local NotifyGroup = Tabs.Settings:AddRightGroupbox("Notifications")
NotifyGroup:AddDropdown("NotifySide", { Text = "Position", Default = "Right", Values = { "TopRight", "BottomRight", "TopLeft", "BottomLeft" }, Callback = function(Value) Window.NotifySide = Value Library:Notify("Position changed to " .. Value) playNotifySound() end })
NotifyGroup:AddInput("SoundId", { Text = "Sound ID", Default = "18317665126", Callback = function(V) Library:Notify("Sound ID updated!") playNotifySound() end })

ThemeManager:SetLibrary(Library)
ThemeManager:ApplyToTab(Tabs.Settings)
SaveManager:SetLibrary(Library)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SetFooter("https://discord.gg/Fnt5g27A")

-- FIX: Watermark FPS & MS Loop (0.5s Update)
task.spawn(function()
    while true do
        local fps = math.floor(Stats.Workspace.Heartbeat:GetValue())
        local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
        Library:SetWatermark(fps .. " FPS  |  Moglimus  |  " .. ping .. " MS")
        task.wait(0.5)
    end
end)

SaveManager:SetFolder("Moglimus/configs")
ThemeManager:SetFolder("Moglimus/themes")
SaveManager:LoadAutoloadConfig()

local loadTime = string.format("%.2f", os.clock() - startTime)
Library:Notify("Welcome " .. Player.DisplayName .. ", loaded in " .. loadTime .. "s")
playNotifySound()
